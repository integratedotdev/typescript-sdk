---
title: "Resend"
description: "Send email and manage domains with the Resend API"
---

The Resend integration provides access to send email and manage domains with the resend api through the Integrate MCP server.

## Installation

```typescript
import { resendIntegration } from "integrate-sdk/server";
```

## Setup

Add Resend to your server configuration. This integration can read credentials from environment variables automatically (`RESEND_API_KEY`):

```typescript
import { createMCPServer, resendIntegration } from "integrate-sdk/server";

export const { client: serverClient } = createMCPServer({
  apiKey: process.env.INTEGRATE_API_KEY,
  integrations: [
    resendIntegration(),
  ],
});
```

### Client-Side Usage

```typescript
import { client } from "integrate-sdk";

const result = await client.resend.listDomains({
  // Add the parameters required by this tool
});

console.log(result);
```

## Configuration Options

<AutoTypeTable
  path="../src/integrations/resend.ts"
  name="ResendIntegrationOptions"
/>

## Environment Variables

- `RESEND_API_KEY`

## Available Tools

- `resend_list_domains`
- `resend_get_domain`
- `resend_create_domain`
- `resend_delete_domain`
- `resend_verify_domain`
- `resend_send_email`
- `resend_get_email`
- `resend_cancel_scheduled_email`

## Notes

- Category: Communication
- Authentication mode: API key
- Tool names and input shapes are defined by the MCP server, so prefer the generated client methods and config table above when wiring this up.

