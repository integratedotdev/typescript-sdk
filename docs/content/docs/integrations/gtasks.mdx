---
title: "Google Tasks Integration"
description: "Manage Google Tasks lists and to-dos synced with your Google account"
---

The Google Tasks integration provides access to manage Google Tasks lists and to-dos synced with your Google account through the Integrate MCP server.

## Installation

The Google Tasks integration is included with the SDK:

```typescript
import { gtasksIntegration } from "integrate-sdk/server";
```

## Setup

### 1. Create a Google Tasks OAuth App

1. Go to [Google Cloud Console](https://console.cloud.google.com)
2. Create a new OAuth application
3. Configure your redirect URI
4. Note your Client ID and Client Secret

### 2. Configure the Integration on Your Server

Add the Google Tasks integration to your server configuration. The integration automatically reads `GTASKS_CLIENT_ID` and `GTASKS_CLIENT_SECRET` from your environment variables:

```typescript
import { createMCPServer, gtasksIntegration } from "integrate-sdk/server";

export const { client: serverClient } = createMCPServer({
  apiKey: process.env.INTEGRATE_API_KEY,
  integrations: [
    gtasksIntegration({
    }),
  ],
});
```

You can override the environment variables by passing explicit values:

```typescript
gtasksIntegration({
  clientId: process.env.CUSTOM_GTASKS_ID,
  clientSecret: process.env.CUSTOM_GTASKS_SECRET,
});
```

### 3. Client-Side Usage

The default client automatically includes all integrations. You can use it directly:

```typescript
import { client } from "integrate-sdk";

await client.authorize("gtasks");
const result = await client.gtasks.clearCompleted({});

console.log(result);
```

If you're using a custom client, add the integration to the integrations array:

```typescript
import { createMCPClient, gtasksIntegration } from "integrate-sdk";

const customClient = createMCPClient({
  integrations: [gtasksIntegration()],
});
```

## Configuration Options

<AutoTypeTable
  path="../src/integrations/gtasks.ts"
  name="GtasksIntegrationConfig"
/>


## Tools

### `gtasks_clear_completed`

Clear completed

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksClearCompletedParams"
/>

### `gtasks_create_task`

Create task

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksCreateTaskParams"
/>

### `gtasks_create_tasklist`

Create tasklist

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksCreateTasklistParams"
/>

### `gtasks_delete_task`

Delete task

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksDeleteTaskParams"
/>

### `gtasks_delete_tasklist`

Delete tasklist

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksDeleteTasklistParams"
/>

### `gtasks_get_task`

Get task

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksGetTaskParams"
/>

### `gtasks_get_tasklist`

Get tasklist

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksGetTasklistParams"
/>

### `gtasks_list_tasklists`

List tasklists

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksListTasklistsParams"
/>

### `gtasks_list_tasks`

List tasks

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksListTasksParams"
/>

### `gtasks_move_task`

Move task

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksMoveTaskParams"
/>

### `gtasks_update_task`

Update task

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksUpdateTaskParams"
/>

### `gtasks_update_tasklist`

Update tasklist

<AutoTypeTable
  path="generated/tool-params/gtasks.ts"
  name="GtasksUpdateTasklistParams"
/>

## Notes

- Category: Productivity
- Authentication mode: OAuth
