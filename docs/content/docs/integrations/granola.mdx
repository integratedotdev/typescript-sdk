---
title: "Granola"
description: "List and read Granola meeting notes and folders"
---

The Granola integration provides access to list and read granola meeting notes and folders through the Integrate MCP server.

## Installation

```typescript
import { granolaIntegration } from "integrate-sdk/server";
```

## Setup

Add Granola to your server configuration. This integration can read credentials from environment variables automatically:

```typescript
import { createMCPServer, granolaIntegration } from "integrate-sdk/server";

export const { client: serverClient } = createMCPServer({
  apiKey: process.env.INTEGRATE_API_KEY,
  integrations: [
    granolaIntegration(),
  ],
});
```

### Client-Side Usage

```typescript
import { client } from "integrate-sdk";

const result = await client.granola.listNotes({
  // Add the parameters required by this tool
});

console.log(result);
```

## Configuration Options

<AutoTypeTable
  path="../src/integrations/granola.ts"
  name="GranolaIntegrationOptions"
/>

## Available Tools

- `granola_list_notes`
- `granola_get_note`
- `granola_list_folders`

## Notes

- Category: Productivity
- Authentication mode: API key
- Tool names and input shapes are defined by the MCP server, so prefer the generated client methods and config table above when wiring this up.

